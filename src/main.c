

int main()
{
    while(1)
    {
    // Read input:
        // Buttons (digital):

        // Joysticks (analogue):

    // Interpret input:
        
    // Update entity models:

    // Update the screen:
    
    }

    // Only return if restarting.
        // Call shutdown functions:

    return 0;
}